using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DoggyController : MonoBehaviour
{
    //[SerializeField] Task2OperatorCallStart task2OperatorCallStart = default;

    void AfterHint155() {
/*
        Animator doggyAnimator = gameObject.GetComponent<Animator>();
        doggyAnimator.ResetTrigger("Wrong");
        doggyAnimator.SetTrigger("Normal");
*/
    }

    void Calling() {
/*
        Animator doggyAnimator = gameObject.GetComponent<Animator>();
        doggyAnimator.ResetTrigger("Wrong");
        doggyAnimator.ResetTrigger("Correct");
        doggyAnimator.ResetTrigger("Normal");
        doggyAnimator.SetTrigger("FadeAway");

        transform.Find("SpeechBubble").gameObject.GetComponent<Animator>().SetTrigger("Close");
*/
        // Start task 2 - call with operator
        //StartCoroutine(task2OperatorCallStart.StartCallWithOperator());
    }

}
